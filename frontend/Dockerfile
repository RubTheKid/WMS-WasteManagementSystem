# Frontend Dockerfile - placeholder for future frontend implementation
FROM node:20-alpine

# Create app directory
WORKDIR /usr/src/app

# Copy package files (when they exist)
# COPY package*.json ./
# RUN npm ci --only=production

# Copy source code
COPY src/ ./src/

# Expose port (adjust as needed for your frontend framework)
EXPOSE 3000

# Start command (adjust based on your frontend framework)
CMD ["echo", "Frontend container ready - implement your frontend framework here"]
